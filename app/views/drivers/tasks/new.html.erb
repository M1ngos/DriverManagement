<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Criar Tarefa</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      min-height: 100vh;
      margin: 0;
      padding: 0;
    }

    .background-radial-gradient {
      background-color: hsl(218, 41%, 15%);
      background-image: radial-gradient(650px circle at 0% 0%,
          hsl(218, 41%, 35%) 15%,
          hsl(218, 41%, 30%) 35%,
          hsl(218, 41%, 20%) 75%,
          hsl(218, 41%, 19%) 80%,
          transparent 100%),
        radial-gradient(1250px circle at 100% 100%,
          hsl(218, 41%, 45%) 15%,
          hsl(218, 41%, 30%) 35%,
          hsl(218, 41%, 20%) 75%,
          hsl(218, 41%, 19%) 80%,
          transparent 100%);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
    }

    .content {
      display: flex;
      justify-content: center;
      width: 100%;
      max-width: 800px;
      margin: auto; /* Center the content */
    }

    .card {
      background-color: hsl(218, 41%, 25%) !important;
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 12px;
      padding: 20px;
      margin-bottom: 20px;
      width: 100%;
      color: white;
    }

    @media (max-width: 850px) {
      .content {
        max-width: 95%;
      }
    }

    .btn-custom {
      background-color: #007bff;
      color: white;
      border: none;
      padding: 10px 10px;
      font-size: 16px;
      border-radius: 8px;
      transition: background-color 0.3s;
    }

    .btn-custom:hover {
      background-color: #0056b3;
    }

    .form-control {
      background-color: hsl(218, 41%, 30%);
      border: 1px solid rgba(255, 255, 255, 0.1);
      color: white;
    }

    .form-control:focus {
      background-color: hsl(218, 41%, 35%);
      color: white;
      border-color: #007bff;
      box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
    }

    label {
      color: white;
    }
  </style>
</head>
<body>
  <div class="background-radial-gradient">
    <div class="content">
      <div class="card">
        <h1 class="h2 fw-bold mb-4" style="color: hsl(218, 81%, 95%)">
          Criar Nova Tarefa
        </h1>
        
        <%= form_with model: @task, url: drivers_tasks_path, local: true do |form| %>
          <div class="mb-3">
            <%= form.label :title, "Título", class: "form-label" %>
            <%= form.text_field :title, class: "form-control", required: true %>
          </div>
          <div class="mb-3">
            <%= form.label :description, "Descrição", class: "form-label" %>
            <%= form.text_area :description, class: "form-control", rows: 4, required: true %>
          </div>
          <div class="mb-3">
            <%= form.submit "Criar Tarefa", class: "btn btn-primary mt-3 w-100" %>
          </div>
        <% end %>

        <%= link_to 'Voltar para Tarefas', driver_dashboard_path, class: 'btn btn-secondary mt-3' %>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>